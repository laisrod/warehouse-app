<%= form_with(model: @order) do |f| %>
  <div class="mb-3 col-md-6">
    <%= f.label :warehouse_id, 'Galpão Destino', class: "form-label" %>
    <%= f.collection_select :warehouse_id, @warehouses, :id, :full_description, {}, class: "form-select" %>
  </div>
  <div class="mb-3 col-md-6">
    <%= f.label :supplier_id, 'Fornecedor', class: "form-label" %>
    <%= f.collection_select :supplier_id, @suppliers, :id, :corporate_name, {}, class: "form-select" %>
  </div>
  <div class="mb-3 col-md-6">
    <%= f.label :estimated_delivery_date, 'Data Prevista de Entrega', class: "form-label" %>
    <%= f.date_field :estimated_delivery_date, class: "form-control" %>
  </div>
  
  <div class="mb-3">
    <%= f.submit 'Gravar', class: "btn btn-dark border-light" %>
  </div>
<% end %>